[project]
name = "boukensha"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv",
    "PyYAML",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
